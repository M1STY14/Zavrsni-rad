services:
  merkle:
    build: .
    container_name: merkle-visualizer
    restart: unless-stopped
    ports:
      - "4000:4000"
    environment:
      NODE_ENV: production
      PORT: 4000
      CORS_ORIGINS: https://mtv.leokocijan.dev
